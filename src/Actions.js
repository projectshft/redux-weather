export const searchTermClick = (searchTerm) => {
  return {
    type: 'SEARCHTERM_CLICK', 
    payload: searchTerm
  }
}

export const fetchPosts = () => {
  const request = 
  
  return {
    type: 'FETCH_POSTS',
    payload: request
  }
}