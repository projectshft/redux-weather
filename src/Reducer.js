const mockState = {
  city: 'Dover'
}

export const inputClick = {
  type: 'INPUT_CLICK'
}

const Reducer = (state = mockState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default Reducer