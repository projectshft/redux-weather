//This component will handle the text input and the search button at the top of the screen

function citySearch() {
    return (
        <div>
            <p>City Search Test</p>
        </div>
    )
}

export default citySearch
